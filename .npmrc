# Disable minimum release age check — pnpm 11.5 supply chain policy holds packages
# for 72h after publish. We own the lockfile and pin exact SHAs; the hold adds no
# meaningful protection here and breaks CI when deps are published the same day.
minimum-release-age=0
