/**
 * Configuración page — placeholder. Full implementation in W7.
 */

export default function ConfiguracionPage() {
  return (
    <div className="px-6 py-8">
      <h1 className="text-xl font-semibold text-slate-900">Configuración</h1>
      <p className="mt-2 text-sm text-slate-500">Variables de entorno (sólo lectura) — próximamente.</p>
    </div>
  );
}
